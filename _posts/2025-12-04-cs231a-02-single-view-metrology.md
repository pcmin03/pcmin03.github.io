---
title: "[CS231A] Lecture 02: Single View Metrology (단일 뷰 측정학)"
categories: [3D Geometry]
tags: [3D Vision, CS231A, Single View Metrology, Computer Vision]
article_header:
  type: overlay
  theme: dark
  background_color: '#0d1b2a'
  background_image:
    gradient: 'linear-gradient(135deg, rgba(13, 27, 42, .85), rgba(176, 125, 103, .65))'
    src: /assets/images/study/3d-geometry.jpg
mathjax: true
mathjax_autoNumber: true
---

**Stanford CS231A: Computer Vision, From 3D Reconstruction to Recognition**

이 포스트는 Stanford CS231A 강의의 두 번째 강의 노트인 "Single View Metrology"를 정리한 것입니다.

**원본 강의 노트**: [02-single-view-metrology.pdf](https://web.stanford.edu/class/cs231a/course_notes/02-single-view-metrology.pdf)

<!--more-->

아래는 **CS231A Lecture 2 – Single View Metrology** 전체를
**전문적이면서도 처음 보는 사람도 이해되도록**,
**스토리텔링 중심**, **"-다"로 끝나는 형태**,
**모든 핵심 수식·그림 설명 포함한 마크다운 정리본**이다.

강의 PDF 전체 내용을 정확히 기반으로 했으며 페이지 번호를 반영해서 설명한다.

---
아래는 **CS231A Lecture 2 – Single View Metrology** 전체를
**전문적이면서도 처음 보는 사람도 이해되도록**,
**스토리텔링 중심**, **“-다”로 끝나는 형태**,
**모든 핵심 수식·그림 설명 포함한 마크다운 정리본**이다.

강의 PDF 전체 내용을 정확히 기반으로 했으며  페이지 번호를 반영해서 설명한다.

---

# 📖 CS231A Lecture 2: Single View Metrology – 완전 정리본

*한 장의 이미지로 3D 세계를 읽어내는 기하학적 마법을 이해하는 강의다.*

이 강의는 다음과 같은 질문에서 시작된다:

> “카메라 내부 파라미터(K)와 외부 파라미터(R,T)를 이미 알고 있다면,
> **한 장의 이미지로부터 3D 공간의 구조를 얼마나 복원할 수 있을까?**”

Lecture 1에서 우리는 **3D → 2D 투영**을 배웠다.
Lecture 2에서는 방향을 바꿔 **2D → 3D 구조 추론**을 다룬다.

이 기술을 **Single View Metrology**라고 부른다.

---

# 1. 왜 Single View Metrology인가?

(PDF p.1) 

사람은 사진 한 장만 보고도 다음과 같은 사실을 추론한다:

* 바닥이 수평이라는 사실
* 벽이 서로 직교한다는 사실
* 건물의 모서리 방향
* 바닥과 벽의 기울기
* 멀고 가까운 길의 방향 변화

이것이 가능한 이유는 **일반적인 장면에는 평행선, 직교 구조, 반복되는 패턴** 등
수많은 3D 구조 힌트가 있기 때문이다.

컴퓨터 비전도 이러한 규칙을 이용하면
**한 장의 이미지에서 길이 비율, 평면 방향, 카메라 자세까지 추정할 수 있게 된다.**

---

# 2. 2D 공간에서의 변환들

(PDF p.1–2) 

3D 정보를 추론하기 위해 먼저 **2D 변환**을 이해해야 한다.
변환은 얼마나 많은 기하학적 제약을 유지하느냐에 따라 다음 순서로 확장된다:

---

## 2.1 Isometric Transformation (등거리 변환)

* 거리 보존
* 거리 보존
* 회전 + 평행이동

$$\begin{bmatrix} x' \\ y' \\ 1 \end{bmatrix} = \begin{bmatrix} R & t \\ 0 & 1 \end{bmatrix} \begin{bmatrix} x \\ y \\ 1 \end{bmatrix}$$

즉, 길이·각도가 항상 그대로 유지된다.

---

## 2.2 Similarity Transformation (유사 변환)

Isometry + **스케일(s)**

$$S = \begin{bmatrix} s & 0 \\ 0 & s \end{bmatrix}$$

$$x' = SRx + t$$

길이 비율과 각도는 유지되지만 크기 자체는 변할 수 있다.

---

## 2.3 Affine Transformation (아핀 변환)

$$x' = Ax + t$$

* 직선 → 직선 보존
* **평행성 유지**
* 거리·각도는 깨진다

Affine은 Similarity보다 더 일반적인 변환이다.

---

## 2.4 Projective Transformation (사영 변환)

가장 일반적인 변환.

$$x' = Hx, \quad H \in \mathbb{R}^{3\times3}$$

특징:

* 선은 여전히 선으로 보낸다
* **평행선은 더 이상 평행이 아닐 수 있다**
* 점/선의 구조적 관계만 남는다

이는 카메라가 세계를 찍을 때 일어나는 실제 변환이다.

---

## 📌 핵심 정리

**Isometry ⊂ Similarity ⊂ Affine ⊂ Projective**

Projective가 가장 일반적이며, Single View Metrology는
이 중 **projective invariance(불변량)** 를 이용하는 학문이다.

---

# 3. Points & Lines at Infinity (무한원점과 무한선)

(PDF p.2–4) 

사진을 보면 평행선이 멀리서 만나듯 보인다.
기하학적으로 이것은 "평행선이 무한대에서 만난다"라고 해석한다.

---

## 3.1 2D 직선 표현

2D line $\ell = (a, b, c)^T$

$$ax + by + c = 0$$

두 직선 $\ell$, $\ell'$의 교점은:

$$x = \ell \times \ell'$$

---

## 3.2 평행선의 교점 = 무한원점

(PDF p.3)

평행선은 유클리드 공간에서는 만나지 않지만,
**프로젝티브 공간에서는 무한대에서 만나는 점을 가진다.**

$\ell = (a,b,c)$, $\ell' = (a', b', c')$

두 직선의 방향이 같다면:

$$\frac{a}{b} = \frac{a'}{b'}$$

교점:

$$x_\infty = \ell \times \ell' \propto (b, -a, 0)$$

마지막 좌표가 0 → **무한대에 있는 점**.

---

## 3.3 모든 평행선 방향마다 무한원점 하나가 존재

(PDF 그림 p.4)

수평선 평행 → 하나의 무한원점
수직선 평행 → 다른 무한원점

그렇게 모인 무한원점들이 만드는 선이 바로 **line at infinity** 이다.

line at infinity $\ell_\infty = (0, 0, 1)^T$

---

## 3.4 Projective 변환은 무한선을 일반선으로 바꿀 수 있다

프로젝티브 변환 $H$을 적용하면:

점 $p_\infty$:

$$p' = Hp_\infty$$

마지막 좌표가 0이 아닐 수 있다 →
무한대에 있던 점이 유한한 위치로 내려온다.

Affine 변환은 유지되지만, projective는 허용된다.

---

# 4. 3D 공간에서의 Vanishing Point, Vanishing Line

(PDF p.5–7) 

## 4.1 3D 평행선 → 이미지에서 하나의 점으로 모임

이 점이 바로 **vanishing point**이다.

3D에서 방향 벡터 $d$를 가진 평행선 무한점은
이미지에서 다음과 같은 점이 된다:

$$v = Kd$$

(식 8, p.5)

즉, 방향 d를 가진 모든 3D 평행선은 하나의 점 v로 모인다.

이를 이용하면 한 장의 사진으로도 다음을 얻는다:

* 방향 정규벡터 d
* 물체의 방향
* 카메라의 회전

---

## 4.2 Vanishing Line (Horizon Line)

(그림 2, p.6)

하나의 평면 위에 여러 방향의 평행선이 있다고 하자.
각 평행선 → 각각 다른 vanishing point 생성.

이 여러 vanishing points를 지나가는 직선이 있다.
이 직선이 바로 **vanishing line** 또는 **horizon line** 이다.

$$\ell_{horiz} = H_P^{-T} \ell_\infty$$

(식 10)

즉, 평면의 "수평선"이 이미지에서 나타나는 선이다.
풍경 사진에서 보이는 **지평선(horizon)** 이 바로 그것이다.

---

## 4.3 Vanishing Line → Plane Normal

(PDF p.6)

평면 $\Pi$의 수직 벡터 $n$은 다음으로 계산된다:

$$n = K^T \ell_{horiz}$$

즉,

* vanishing line을 이미지에서 검출
* 카메라 K를 알고 있다면
  → **평면의 3D normal을 알 수 있다**

---

## 4.4 두 방향 벡터 사이 각도

(PDF p.7 그림 4)

서로 다른 두 방향 $d_1$, $d_2$
각각의 vanishing point $v_1$, $v_2$ 이용하면

$$\cos\theta = \frac{v_1^T \omega v_2}{\sqrt{v_1^T \omega v_1}\sqrt{v_2^T\omega v_2}}$$

여기서:

$$\omega = (KK^T)^{-1}$$

즉, **카메라 캘리브레이션이 되면**,
이미지 속 두 방향의 실제 3D 각도를 잴 수 있다.

---

## 4.5 두 평면 사이 각도

(PDF p.8)

각 평면의 horizon line $\ell_1$, $\ell_2$로부터 normal을 계산한 후:

$$\cos\theta= \frac{\ell_1^T \omega^{-1}\ell_2}{\sqrt{\ell_1^T\omega^{-1}\ell_1}\sqrt{\ell_2^T\omega^{-1}\ell_2}}$$

→ 이미지 안에서 **벽·바닥·경사면의 각도를 모두 구할 수 있다**.

---

# 5. Single View Metrology 예제

(PDF p.8–9) 

강의에서는 그림 5–6처럼
건물 사진에서 세 개의 직교 방향을 찾는 예제를 든다.

---

## 5.1 세 방향이 서로 직교한다고 가정

즉, 건물의:

* 가로 방향
* 세로 방향
* 높이 방향

이 3D에서 서로 직교한다고 안다면,

각각의 평행선 집합 → vanishing point $(v_1, v_2, v_3)$ 검출

---

## 5.2 직교 조건

각각의 방향 벡터는 서로 직교하므로:

$$v_1^T \omega v_2 = 0 \\ v_1^T \omega v_3 = 0 \\ v_2^T \omega v_3 = 0$$

3개의 식 획득.

---

## 5.3 카메라 내부 파라미터 K 복원

그러나 문제는:

* K는 5 DoF
* 식은 3개 → 부족함

하지만 다음 두 가지를 가정하면 충분해진다:

1. zero skew
2. square pixels ($f_x \approx f_y$)

이 때 $\omega = (KK^T)^{-1}$의 형태는 다음처럼 단순해진다:

$$\omega = \begin{bmatrix} \omega_1 & 0 & \omega_4 \\ 0 & \omega_1 & \omega_5 \\ \omega_4 & \omega_5 & \omega_6 \end{bmatrix}$$

scale ambiguity 때문에 실제 변수는 3개뿐.

3개의 직교 조건식 → 3개의 미지수 해결 가능.

---

## 5.4 최종적으로 K 복원 가능

(Cholesky 분해로 K 추출 가능)

즉, **한 장의 사진만으로 카메라 내부 파라미터를 추정할 수 있다.**
그 후:

* 모든 평면의 normal 계산
* 3D 구조 재구성 가능
* 길이, 각도, 비율 측정 가능

이것이 Single View Metrology의 핵심이다.

---

# 6. 이 강의의 핵심 철학

사진은 단순한 2D 픽셀 배열처럼 보이지만,
그 뒤에는 **세계의 기하학적 규칙**이 숨어 있다.

* 평행선은 vanishing point를 만든다
* 평면의 집합은 horizon line을 만든다
* vanishing point의 조합은 3D 방향을 알려준다
* 3개의 직교 vanishing point는 카메라 K를 알려준다
* K를 알면 plane normal을 알 수 있다
* plane normal을 알면 평면의 기울기, 물체 방향을 알 수 있다

즉,

> **사진 한 장으로도 세계의 3D 구조를 읽어낼 수 있다.**

Single View Metrology는
바로 이 믿기 어려운 사실을 수학적으로 가능하게 한다.

---

# 7. 이어서 학습하면 좋을 다음 Lecture

Lecture 3에서는 **Two-View Geometry** 로 넘어가
Epipolar Geometry, Fundamental Matrix 등을 다루게 된다.

---

## 1. 서론 (Introduction)

단일 뷰 측정학(Single View Metrology)은 **단일 2D 이미지에서 3D 세계의 기하학적 정보를 추출하고 측정하는 기술**입니다. 카메라의 내부 및 외부 파라미터, 그리고 이미지 내의 소실점(vanishing points)과 소실선(vanishing lines)을 활용하여 이루어집니다.

## 2. 소실점과 소실선 (Vanishing Points and Lines)

### 2.1. 소실점의 개념

평행한 3D 선들은 카메라 투영 후 2D 이미지 평면에서 한 점으로 수렴합니다. 이 점을 **소실점(Vanishing Point)**이라고 합니다.

![Vanishing Points](/assets/images/posts/cs231a-02/figures/page_4_img_1.png)

### 2.2. 소실점의 수학적 표현

3D 공간에서 방향 벡터 $D = [D_X, D_Y, D_Z]^T$를 가진 평행한 선들은 이미지 평면에서 단일 소실점 $v$로 투영됩니다:

$$v \sim K R D$$

여기서 $K$는 카메라 내부 파라미터 행렬, $R$은 회전 행렬입니다.

### 2.3. 소실선 (Vanishing Lines)

동일 평면상의 평행선들이 형성하는 소실점들을 연결하면 **소실선(Vanishing Line)**이 됩니다.

## 3. 단일 이미지에서의 측정 (Metric Measurements from a Single Image)

### 3.1. 길이 측정 (Length Measurement)

이미지 내에서 알려진 길이의 기준선(reference length)이 있을 경우, 이 기준선과 평행한 다른 선들의 길이를 측정할 수 있습니다.

두 점 $P_1, P_2$의 3D 길이를 측정하기 위해, 이들이 소실점 $v$를 향하는 선 위에 있다고 가정합니다:

$$L = L_{ref} \frac{d(p_1, v)}{d(p_2, v)}$$

여기서 $L_{ref}$는 기준 길이, $d(\cdot, \cdot)$는 이미지 상의 거리입니다.

### 3.2. 각도 측정 (Angle Measurement)

두 선 사이의 3D 각도를 측정하기 위해, 이 선들의 소실점을 활용합니다. 두 선의 소실점 $v_1, v_2$와 카메라의 내부 파라미터 $K$가 주어졌을 때, 두 선 사이의 각도 $\theta$는 다음과 같이 계산됩니다:

$$\cos \theta = \frac{v_1^T (K K^T)^{-1} v_2}{\sqrt{v_1^T (K K^T)^{-1} v_1} \sqrt{v_2^T (K K^T)^{-1} v_2}}$$

### 3.3. 비율 측정 (Ratio Measurement)

동일한 3D 선 위에 있는 점들의 비율은 이미지 평면에서도 보존됩니다.

## 4. 카메라 캘리브레이션 (Camera Calibration)

단일 뷰 측정학을 정확하게 수행하기 위해서는 카메라의 내부 파라미터 $K$를 아는 것이 중요합니다.

### 4.1. 직교 소실점 (Orthogonal Vanishing Points)

3D 공간에서 서로 직교하는 세 방향의 평행선들이 이미지 평면에 투영되면 세 개의 소실점을 형성합니다. 이 소실점들을 활용하여 카메라의 내부 파라미터를 추정할 수 있습니다.

![Orthogonal Vanishing Points](/assets/images/posts/cs231a-02/figures/page_6_img_1.png)
![Single View Metrology](/assets/images/posts/cs231a-02/figures/page_7_img_1.png)
![Measurement Examples](/assets/images/posts/cs231a-02/figures/page_7_img_2.png)

세 개의 직교 소실점 $v_1, v_2, v_3$가 주어졌을 때:

$$v_i^T (K K^T)^{-1} v_j = 0 \quad \text{for } i \neq j$$

이 관계를 통해 $K K^T$ 행렬을 복원하고, Cholesky 분해를 통해 $K$를 얻을 수 있습니다.

## 5. 응용 분야 (Applications)

단일 뷰 측정학은 다음과 같은 분야에서 활용됩니다:

- **증강 현실 (Augmented Reality)**: 실제 장면에 가상 객체를 정확하게 배치
- **범죄 현장 재구성 (Crime Scene Reconstruction)**: 단일 사진에서 거리 및 크기 추정
- **건축 및 도시 계획 (Architecture and Urban Planning)**: 건물 높이 및 구조 측정
- **자율 주행 (Autonomous Driving)**: 도로 표지판 및 객체 크기 추정

## 요약

이 강의에서는 다음과 같은 내용을 다뤘습니다:

1. **소실점과 소실선**: 평행선이 수렴하는 점과 선
2. **길이 측정**: 기준선을 이용한 3D 길이 측정
3. **각도 측정**: 소실점을 이용한 3D 각도 측정
4. **카메라 캘리브레이션**: 직교 소실점을 이용한 내부 파라미터 추정

단일 뷰 측정학은 단일 2D 이미지에서 풍부한 3D 기하학적 정보를 추출할 수 있는 강력한 도구입니다.

## 참고 자료

- [Stanford CS231A Course Notes](https://web.stanford.edu/class/cs231a/course_notes.html)
- [02-single-view-metrology.pdf](https://web.stanford.edu/class/cs231a/course_notes/02-single-view-metrology.pdf)
